import { Routes } from '@angular/router';
import { MainPageComponent } from './features/common/pages/main-page/main-page.component';

export const routes: Routes = [
    { path: '', component: MainPageComponent }
];
